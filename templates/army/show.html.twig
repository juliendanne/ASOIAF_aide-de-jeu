{% extends 'base.html.twig' %}

{% block title %}Army{% endblock %}

{% block body %}
    <h1>Army</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ army.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ army.name }}</td>
            </tr>
            <tr>
                <th>Limit_cost</th>
                <td>{{ army.limitCost }}</td>
            </tr>
            <tr>
                <th>Total_cost</th>
                <td>{{ army.totalCost }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_army_index') }}">back to list</a>

    <a href="{{ path('app_army_edit', {'id': army.id}) }}">edit</a>

    {{ include('army/_delete_form.html.twig') }}
{% endblock %}
