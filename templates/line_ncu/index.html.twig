{% extends 'base.html.twig' %}

{% block title %}LineNCU index{% endblock %}

{% block body %}
    <h1>LineNCU index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for line_n_c_u in line_n_c_us %}
            <tr>
                <td>{{ line_n_c_u.id }}</td>
                <td>
                    <a href="{{ path('app_line_n_c_u_show', {'id': line_n_c_u.id}) }}">show</a>
                    <a href="{{ path('app_line_n_c_u_edit', {'id': line_n_c_u.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="2">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_line_n_c_u_new') }}">Create new</a>
{% endblock %}
