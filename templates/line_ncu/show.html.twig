{% extends 'base.html.twig' %}

{% block title %}LineNCU{% endblock %}

{% block body %}
    <h1>LineNCU</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ line_n_c_u.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_line_n_c_u_index') }}">back to list</a>

    <a href="{{ path('app_line_n_c_u_edit', {'id': line_n_c_u.id}) }}">edit</a>

    {{ include('line_ncu/_delete_form.html.twig') }}
{% endblock %}
