{% extends 'base.html.twig' %}

{% block title %}TypeOfUnit{% endblock %}

{% block body %}
    <h1>TypeOfUnit</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ type_of_unit.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ type_of_unit.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_type_of_unit_index') }}">back to list</a>

    <a href="{{ path('app_type_of_unit_edit', {'id': type_of_unit.id}) }}">edit</a>

    {{ include('type_of_unit/_delete_form.html.twig') }}
{% endblock %}
