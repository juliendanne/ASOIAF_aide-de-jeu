<form method="post" action="{{ path('app_no_combat_unit_delete', {'id': no_combat_unit.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ no_combat_unit.id) }}">
    <button class="btn">Delete</button>
</form>
