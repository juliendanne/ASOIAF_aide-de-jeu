{% extends 'base.html.twig' %}

{% block title %}NoCombatUnit{% endblock %}

{% block body %}
    <h1>NoCombatUnit</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ no_combat_unit.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ no_combat_unit.name }}</td>
            </tr>
            <tr>
                <th>Full_name</th>
                <td>{{ no_combat_unit.fullName }}</td>
            </tr>
            <tr>
                <th>Is_a_character</th>
                <td>{{ no_combat_unit.isACharacter ? 'Yes' : 'No' }}</td>
            </tr>
            <tr>
                <th>Cost</th>
                <td>{{ no_combat_unit.cost }}</td>
            </tr>
            <tr>
                <th>Card</th>
                <td>{{ no_combat_unit.card }}</td>
            </tr>
            <tr>
                <th>CardVerso</th>
                <td>{{ no_combat_unit.cardVerso }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_no_combat_unit_index') }}">back to list</a>

    <a href="{{ path('app_no_combat_unit_edit', {'id': no_combat_unit.id}) }}">edit</a>

    {{ include('no_combat_unit/_delete_form.html.twig') }}
{% endblock %}
