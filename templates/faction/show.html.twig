{% extends 'base.html.twig' %}

{% block title %}Faction{% endblock %}

{% block body %}
    <h1>Faction</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ faction.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ faction.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_faction_index') }}">back to list</a>

    <a href="{{ path('app_faction_edit', {'id': faction.id}) }}">edit</a>

    {{ include('faction/_delete_form.html.twig') }}
{% endblock %}
