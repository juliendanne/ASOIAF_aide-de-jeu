<form method="post" action="{{ path('app_commander_delete', {'id': commander.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ commander.id) }}">
    <button class="btn">Delete</button>
</form>
