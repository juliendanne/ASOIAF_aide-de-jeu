{% extends 'base.html.twig' %}

{% block title %}LineCommander index{% endblock %}

{% block body %}
    <h1>LineCommander index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for line_commander in line_commanders %}
            <tr>
                <td>{{ line_commander.id }}</td>
                <td>
                    <a href="{{ path('app_line_commander_show', {'id': line_commander.id}) }}">show</a>
                    <a href="{{ path('app_line_commander_edit', {'id': line_commander.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="2">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_line_commander_new') }}">Create new</a>
{% endblock %}
