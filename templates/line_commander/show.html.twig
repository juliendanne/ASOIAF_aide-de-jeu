{% extends 'base.html.twig' %}

{% block title %}LineCommander{% endblock %}

{% block body %}
    <h1>LineCommander</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ line_commander.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_line_commander_index') }}">back to list</a>

    <a href="{{ path('app_line_commander_edit', {'id': line_commander.id}) }}">edit</a>

    {{ include('line_commander/_delete_form.html.twig') }}
{% endblock %}
