{% extends 'base.html.twig' %}

{% block title %}LineArmy index{% endblock %}

{% block body %}
    <h1>LineArmy index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Quantity</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for line_army in line_armies %}
            <tr>
                <td>{{ line_army.id }}</td>
                <td>{{ line_army.quantity }}</td>
                <td>
                    <a href="{{ path('app_line_army_show', {'id': line_army.id}) }}">show</a>
                    <a href="{{ path('app_line_army_edit', {'id': line_army.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_line_army_new') }}">Create new</a>
{% endblock %}
