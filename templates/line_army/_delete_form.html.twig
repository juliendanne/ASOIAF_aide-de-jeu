<form method="post" action="{{ path('app_line_army_delete', {'id': line_army.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ line_army.id) }}">
    <button class="btn">Delete</button>
</form>
