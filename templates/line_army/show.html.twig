{% extends 'base.html.twig' %}

{% block title %}LineArmy{% endblock %}

{% block body %}
    <h1>LineArmy</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ line_army.id }}</td>
            </tr>
            <tr>
                <th>Quantity</th>
                <td>{{ line_army.quantity }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_line_army_index') }}">back to list</a>

    <a href="{{ path('app_line_army_edit', {'id': line_army.id}) }}">edit</a>

    {{ include('line_army/_delete_form.html.twig') }}
{% endblock %}
