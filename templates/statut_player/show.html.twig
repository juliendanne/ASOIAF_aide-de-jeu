{% extends 'base.html.twig' %}

{% block title %}StatutPlayer{% endblock %}

{% block body %}
    <h1>StatutPlayer</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ statut_player.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ statut_player.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_statut_player_index') }}">back to list</a>

    <a href="{{ path('app_statut_player_edit', {'id': statut_player.id}) }}">edit</a>

    {{ include('statut_player/_delete_form.html.twig') }}
{% endblock %}
