<form method="post" action="{{ path('app_statut_player_delete', {'id': statut_player.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ statut_player.id) }}">
    <button class="btn">Delete</button>
</form>
