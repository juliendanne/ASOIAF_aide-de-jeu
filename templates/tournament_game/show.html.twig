{% extends 'base.html.twig' %}

{% block title %}TournamentGame{% endblock %}

{% block body %}
    <h1>TournamentGame</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ tournament_game.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ tournament_game.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_tournament_game_index') }}">back to list</a>

    <a href="{{ path('app_tournament_game_edit', {'id': tournament_game.id}) }}">edit</a>

    {{ include('tournament_game/_delete_form.html.twig') }}
{% endblock %}
